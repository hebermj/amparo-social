:80 {
	# Portão de autenticação: só o Amparo (que conhece a credencial) acessa.
	# SEARXNG_USER/SEARXNG_PASSWORD_BCRYPT vêm do .env (compose).
	basic_auth {
		{$SEARXNG_USER} {$SEARXNG_PASSWORD_BCRYPT}
	}

	header X-Robots-Tag "noindex, nofollow"

	reverse_proxy searxng:8080
}
